{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力：允许调用 Tauri 核心 API、更新插件与本应用自定义命令（自定义命令仅对本地启动页生效；Harness 远程页面的重启走 on_navigation 拦截，不走 IPC）",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
